package com.example.diapplication.common

enum class UiError(val message: String) {
    CITY_ALREADY_EXISTS("City already added"),
    CITY_NOT_FOUND("City not found, please check correctness of city name"),
}